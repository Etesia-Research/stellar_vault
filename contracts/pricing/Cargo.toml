[package]
name = "etesia-pricing"
version = "0.1.0"
edition = "2021"
publish = false
license-file = "../../LICENSE"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
etesia-vault = { path = "../vault" }
